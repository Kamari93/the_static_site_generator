This Project converts Markdown into HTML. The static site generator takes a directory of Markdown files (one for each web page), and builds a directory of HTML files. 

[Live Demo](https://kamari93.github.io/the_static_site_generator/)